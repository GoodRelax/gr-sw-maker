# 用語集の適用状態

**`framework-src/{lang}/process-rules/glossary.md` について、何を当てたか・何が残っているかを持つ。用語集に関することは本書が正である。** `03-work-order.md` は用語集以外への波及だけを持ち、本書を参照する。

**`14-framework-update-plan.md` の手順 1 を 2 段に割って実行した。**

**対象:** `framework-src/ja/process-rules/glossary.md` と `framework-src/en/process-rules/glossary.md`（どちらも 173 → 200 行）。

> **`14` の手順 10「en 版は ja が全部終わってから」は採らない。** `framework-development.md` §5.1 が「**片方の言語だけを変えるコミットを作ってはならない（MUST）**」と定めており、pre-commit フックがそれを強制する。**手順ごとにコミットする限り、`14` の順序は必ず規則違反のコミットを生む。** 経緯は `History/2026-08-10/06-glossary-1a-record.md`。

> **現在は利用者の指示により日英の比較を止めている**（`03-work-order.md` §11.1）。**ja を先に直し、再開時に en へ一括で当てる。**

---

## 2. 手順 1a で当てたもの

### 2.1 §2 フレームワーク固有概念

**仕様形式の 3 行を書き換えた:**

| 行 | 変更 |
|---|---|
| `ANMS` | 「単一 Markdown ファイル」→「1 枚に収める形式。**記法は ANPS と同じだが StrictDoc を回さない**」 |
| `ANPS` | **`ANPS-part` と `ANPS-chapter` の 2 行に分けた** |
| `ANGS` | **廃止と明記した**（行は残す。消すと廃止した記録が消える） |

**新語 19 語を登録した:** 機器 / 経路 / ノード / SW_SPEC・SWS / ドメインモデル / データスキーマ / コンポーネント図 / 削減候補 / 手段独立テスト / 精密度 / 目標レベル / 記述文 / 指示文 / 与件 / review / check / audit / test-designer / tester。

**`review` / `check` / `audit` の定義は `02-framework-fixes.md` §8.1 の案をそのまま採った。**

### 2.2 §4 紛らわしい対

**7 対を登録した。**

| 対 | 何を防ぐか |
|---|---|
| actor vs Chapter 3 のアクター | Common Block のフィールドと、目標を持つ主体 |
| Use Case vs ユースケース | CA の層の名前と、アクターの目標 |
| **機器 vs 機械** | **Chapter 2 の実体と、「機械可読」の機械** |
| **ノード vs ND** | **StrictDoc のノードと、廃止した接頭辞** |
| 経路 vs 接続 | 一方向の道すじと、資源の一般語 |
| ドメインモデル vs データスキーマ | Chapter 5 の概念と、Chapter 6 の具体構造 |
| review vs check vs audit | 誰が / 何を見るか / いつ |

### 2.3 章番号

**2 件。`01-chapter-number-census.md` の順序（節 → 章）で当てた。**

| 行 | 旧 | 新 |
|---|---|---|
| 119 | `spec-template Ch3.1` | **`Ch5.1`** |
| 25 | FMEA「`Ch3` 確定後に実施」 | **`Ch5`** |

---

## 4. 非採用欄に置けない語（重要）

> **`check-terms.mjs` は CJK を単語境界なしの部分一致で照合する。** 部分文字列として正当な用法に現れる語を非採用欄に置くと、その用法まで違反になる。

| 語 | 置けない理由 |
|---|---|
| **機械** | 「機械可読」「機械が検証する」の 22 箇所が違反になる |
| **ノード** | StrictDoc のノードの全用例が違反になる |
| **接続** | 「接続・ファイル・メモリ」（資源管理）等が違反になる |
| **テンプレート**（単独） | **`CLAUDE.md テンプレート` `Common Block テンプレート` `incident-report テンプレート` など別の雛形を指す 42 件が違反になる** |

**これらは §4「紛らわしい対」で区別する。非採用欄には置かない（MUST NOT）。**

### 4.1 Latin の語は大文字小文字を区別しない

> **`check-terms.mjs` は Latin の語を `\b...\b` の単語境界つき・`i` フラグで照合する**（`occurrences()`）。**CJK と違って部分一致はしないが、大文字小文字は区別しない。**

区分名の 4 語を非採用にすると、英文の一般語としての用法が巻き添えになる。**実測値は次のとおり**（フェンス内と ALLOWED 4 ファイルを除いた、`check-terms` と同じ条件）。

| 語 | 大文字始まり（区分名） | それ以外（英文の一般語） | 判定 |
|---|---:|---:|---|
| `Micro` | 10 | **0** | **そのまま非採用にできる** |
| `Small` | 4 | **4** | 一般語を先に読み分ける |
| `Standard` | 64 | **26** | 一般語を先に読み分ける |
| `Large` | 7 | **13** | **一般語のほうが多い。慎重に** |

**区分名としての用法（大文字始まり 85 件）は段 1 で `簡易` / `標準` / `厳格` へ置き換わって消える。** 残る一般語 43 件を読み分けてから武装する。**「置き換えたら 0 件になった」で武装してはならない。** 大文字始まりが消えても、小文字の一般語が残る。

---

## 5. 手順 1b で武装する語

**§1 の非採用欄へ登録する。前提の作業を終えてから武装する。**

| 採用語 | 非採用 | 前提 |
|---|---|---|
| ドメインモデル / データスキーマ | **データモデル** | `document-rules:1273, 1277` ・ `process-rules:900, 913` ・ `agents/architect.md` の description |
| ANMS / ANPS-part / ANPS-chapter | **ANGS** | `document-rules` 5 件 ・ `process-rules` 7 件 ・ `CLAUDE.md` ・ `agents/srs-writer.md` ・ `spec-template.md` |
| **仕様書テンプレート** | **骨格** | **無し。`framework-src/` に 0 件。今すぐ武装できる** |
| **仕様書テンプレート** | **仕様テンプレート** | `agents/architect.md:106` ・ `agents/srs-writer.md:95`（各 2 言語）。`仕様書テンプレート` は `仕様テンプレート` を部分文字列として含まないので、直した後なら安全 |

**`ANGS` は `commands/council-review.md` にも 1 件あるが、同ファイルは `check-terms` の ALLOWED に入っている。** 武装しても落ちないが、**文面は直す。**

---

## 5.1 用語集へ足す定義（2026-08-10 の決定）

| # | 節 | 内容 |
|:-:|---|---|
| 1 | §2 | **`仕様書テンプレート（Specification Template）` を登録する。** `テンプレート` だけでは何の雛形か伝わらないため、語自体に対象を持たせる |
| 2 | §4 紛らわしい対（現 `:106`） | 「仕様書（Specification） vs テンプレート（Template）」→「**仕様書 vs 仕様書テンプレート**」。単独の `テンプレート` は他の雛形にも使う一般語であると明記する |
| 3 | §1 | `簡易` / `標準` / `厳格` を採用語として登録し、**`Micro` / `Small` / `Standard` / `Large` / `中規模以上` / `通常` / `厳密` の 7 語**を非採用として登録する（`00-mode-matrix.md` §10 検査 3 と同じ 7 語）。**実測で `簡易` `厳格` は用語集に 0 件である。** 武装の可否は §4.1 |
| 4 | `:48` | 「ID 接頭辞の正本は spec-template の接頭辞表」と書いてあるが、**新しい仕様書テンプレートに接頭辞表は無い**（`02-spec-writing-rules.md` の「ID の接頭辞」節にある）。ポインタの行き先を移す |
| 5 | 仕様形式の枚数 | ANPS の枚数が 3 枚 / 14 枚と書かれている。**正は 4 枚 / 14 枚**（Chapter 8 削除で 15 → 14 になった。数が偶然一致するので、根拠まで確かめる） |
| 6 | §2 | **エージェント連携の語を登録する。正本は `07-agent-orchestration-rules.md` §1.1 であり、日英の対もそこが持つ。** 生存状態: `起動`(start) / `背景起動`(background start) / `完了`(finish) / `待機`(standby) / `再開`(resume) / `停止`(stop)。費用: `文脈`(context) / `基盤層`(base layer) / `会話層`(conversation layer) / `下限`(floor) / `キャッシュ`(cache) / `キャッシュの寿命`(cache TTL) / `ウォーム待機`(warm standby) / `コールド待機`(cold standby) / `キープアライブ`(keep-alive) / `往復`(round trip) |
| 7 | §1 非採用 | **`保温` を非採用にする**（`キープアライブ` へ寄せた。`framework-src/` に 0 件なので今すぐ武装できる）。**`中断` は非採用にできない** —— 「作業が中断する」の一般語として使うため（§4 の制約と同じ形）|
| 8 | §4 紛らわしい対 | **「キャッシュの寿命 vs エージェントが在り続ける長さ」を足す。** 前者は 5 分の TTL、後者はパイプライン上でそのエージェントが必要とされる長さで、**どちらも「寿命」と呼ばれていた**（`07` §1.1・§3.5） |
| 9 | §4 紛らわしい対 | **「待機（standby）vs 中断（suspend）」を足す。** 待機は完了して次を待つ状態であり、**走行を凍らせる中断という状態は存在しない**（`07` §1.1 の「使わない語」） |

---

