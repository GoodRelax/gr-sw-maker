# 手順 1（用語集）の記録

**`14-framework-update-plan.md` の手順 1 を 2 段に割って実行した。本書は何を当て、何が残っているかの記録である。**

**対象:** `framework-src/ja/process-rules/glossary.md` と `framework-src/en/process-rules/glossary.md`（どちらも 173 → 200 行）。

> **`14` の手順 10「en 版は ja が全部終わってから」は採らない。** `framework-development.md` §5.1 が「**片方の言語だけを変えるコミットを作ってはならない（MUST）**」と定めており、pre-commit フックがそれを強制する。**手順ごとにコミットする限り、`14` の順序は必ず規則違反のコミットを生む。** §7 を見よ。

---

## 1. なぜ 2 段に割ったか

**`14` の手順 1 には解けない矛盾がある。**

| 何 | 内容 |
|---|---|
| 手順 1-2 | 「データモデル」を**非採用語にする** |
| 手順 1 の確認 | **`node tools/check-terms.mjs` が通る** |

**`check-terms.mjs` は用語集 §1 の「非採用」欄を検査リストとして読み、その語が `framework-src` のどこかに現れたら落とす。** 非採用欄は記録ではなく、実行される規則である。

**「データモデル」は 5 箇所 × 2 言語に現存する** —— `agents/architect.md`（description）、`process-rules/full-auto-dev-document-rules.md:1273, 1277`、`process-rules/full-auto-dev-process-rules.md:900, 913`。**これらを直すのは手順 4・5・9 であり、前倒しすると手順 4 冒頭の「同じファイルを 2 度開かない」に反する。**

> **「用語を固める」ことと「非採用欄で強制する」ことは分けられる。** 語を消し終えてから武装するのが順序である。

| 段 | 何を | 状態 |
|:-:|---|---|
| **1a** | 定義を置く。§2 に新語、§4 に紛らわしい対、章番号 2 件 | **完了** |
| **1b** | **非採用欄を武装する** | **手順 9 の後、手順 10 の前に行う** |

---

## 2. 手順 1a で当てたもの

### 2.1 §2 フレームワーク固有概念

**仕様形式の 3 行を書き換えた:**

| 行 | 変更 |
|---|---|
| `ANMS` | 「単一 Markdown ファイル」→「1 枚に収める形式。**記法は ANPS と同じだが StrictDoc を回さない**」 |
| `ANPS` | **`ANPS-part` と `ANPS-chapter` の 2 行に分けた** |
| `ANGS` | **廃止と明記した**（行は残す。消すと廃止した記録が消える） |

**新語 19 語を登録した:** 機器 / 経路 / ノード / SW_SPEC・SWS / ドメインモデル / データスキーマ / コンポーネント図 / 削減候補 / 手段独立テスト / 精密度 / 目標レベル / 記述文 / 指示文 / 与件 / review / check / audit / test-designer / tester。

**`review` / `check` / `audit` の定義は `02-framework-fixes.md` §8.1 の案をそのまま採った。**

### 2.2 §4 紛らわしい対

**7 対を登録した。**

| 対 | 何を防ぐか |
|---|---|
| actor vs Chapter 3 のアクター | Common Block のフィールドと、目標を持つ主体 |
| Use Case vs ユースケース | CA の層の名前と、アクターの目標 |
| **機器 vs 機械** | **Chapter 2 の実体と、「機械可読」の機械** |
| **ノード vs ND** | **StrictDoc のノードと、廃止した接頭辞** |
| 経路 vs 接続 | 一方向の道すじと、資源の一般語 |
| ドメインモデル vs データスキーマ | Chapter 5 の概念と、Chapter 6 の具体構造 |
| review vs check vs audit | 誰が / 何を見るか / いつ |

### 2.3 章番号

**2 件。`01-chapter-number-census.md` の順序（節 → 章）で当てた。**

| 行 | 旧 | 新 |
|---|---|---|
| 119 | `spec-template Ch3.1` | **`Ch5.1`** |
| 25 | FMEA「`Ch3` 確定後に実施」 | **`Ch5`** |

---

## 3. `14` の手順 1 との差分

**手順 1-6 の登録語リストは、v0.36 の決定で古くなっていた。**

| `14` の指示 | 実際に採った扱い |
|---|---|
| 「ノード」を新語として登録 | **StrictDoc のノードとして登録し、廃止した `ND` との対を §4 に置いた。** v0.35 の「ノード（機器）」は存在しない |
| 「接続」を新語として登録 | **「経路」として登録した。** Chapter 2 は `CN` を廃止し、語も経路に変えた |
| 「機械」を新語として登録 | **「機器」として登録した。** 2026-08-09 に機械 → 機器へ改名済み（59 箇所） |

**手順 1 が挙げていないが必要だったもの:** `ANGS` の廃止と `ANPS` の 2 分割。用語集 §2 が 3 形式のままだった。

---

## 4. 非採用欄に置けない語（重要）

> **`check-terms.mjs` は CJK を単語境界なしの部分一致で照合する。** 部分文字列として正当な用法に現れる語を非採用欄に置くと、その用法まで違反になる。

| 語 | 置けない理由 |
|---|---|
| **機械** | 「機械可読」「機械が検証する」の 22 箇所が違反になる |
| **ノード** | StrictDoc のノードの全用例が違反になる |
| **接続** | 「接続・ファイル・メモリ」（資源管理）等が違反になる |

**これらは §4「紛らわしい対」で区別する。非採用欄には置かない（MUST NOT）。**

---

## 5. 手順 1b で武装する語

**手順 9 の後、手順 10 の前に、§1 の非採用欄へ登録する。**

| 採用語 | 非採用 | 前提となる手順 |
|---|---|---|
| ドメインモデル / データスキーマ | **データモデル** | 手順 4（document-rules:1273, 1277）・手順 5（process-rules:900, 913）・手順 9（architect.md の description） |
| ANMS / ANPS-part / ANPS-chapter | **ANGS** | 手順 4（document-rules 5 件）・手順 5（process-rules 7 件）・手順 8（CLAUDE.md）・手順 9（srs-writer.md）・手順 2（spec-template） |

**`ANGS` は `commands/council-review.md` にも 1 件あるが、同ファイルは `check-terms` の ALLOWED に入っている。** 武装しても落ちないが、**文面は直す。**

---

## 6. 検査の状態

| 検査 | 状態 |
|---|---|
| **check-terms** | **PASS**（30 語 / 74 ファイル）—— 手順 1 の確認 |
| check-parity | **PASS**（41 ファイル対） |
| check-links | PASS |
| check-tagnames | PASS |
| check-roster | PASS |
| check-setup | PASS |
| **pre-commit フック** | **PASS**（両言語を staged にして実行） |

---

## 7. 作業順序の変更 —— ja と en を同時に直す

**`14` の手順 10 は、リポジトリ自身の MUST 規則と両立しない。**

| 出どころ | 言っていること |
|---|---|
| `14` 手順 10 | 「en 版は ja が全部終わってから。`check-parity` が 41 ファイル対で行数一致を強制する」 |
| **`framework-development.md` §5.1** | **「片方の言語だけを変えるコミットを作ってはならない（MUST）」** |

**§5.1 の理由:** 分かれてしまうと、次に読む者はどちらが正でどちらが未更新かを判別できず、ずれが恒久化する。**`tools/hooks/pre-commit` がこれを強制する。**

**`--no-verify` は使えない。** フック自身が「counterpart genuinely needs no change のときだけ」と限定しており、本件は en に実際の変更が要った。

**したがって順序を変える:**

| | `14` の想定 | **採る順序** |
|---|---|---|
| 各手順 | ja のみを直す | **ja と en を同時に直す** |
| 手順 10 | en へ一括反映（41 ファイル対） | **最終確認に格下げする** |

> **`14` の想定より楽である。** 41 ファイル分の行数合わせを最後にまとめて行うより、1 ファイルずつその場で合わせるほうが確実で、**各手順の終わりで `check-parity` が緑のままになる。** `14` が ja 先行を選んだ理由（行数合わせの手間）は、まとめてやるという前提の下でのみ成り立っていた。

**`14` の手順 10 の記述は、この発見を反映して書き換える必要がある。未実施。**
