# 表の作り直し案（2026-08-10）

**`00-mode-matrix.md` の表 C と表 D-1 の差し替え案である。** OK なら 00 へ統合し、本書は `History/` へ移す。

**方式ごとの値は 1 セルも変えていない。** 変えるのは列の構成と備考だけである。

---

## 1. 表の作り方 —— 4 つの原則

| # | 原則 | 根拠 |
|:-:|---|---|
| 1 | **1 枚の表は「1 本の軸 × 開発方式」だけを持つ** | 00 の全表が既にこの形。方式の列を持たない関係は 00 に置かない |
| 2 | **関係は列にする。備考に書かない** | 備考は条件と例外の場所。列にすれば検査できる |
| 3 | **識別列は複数持ってよい。方式の列だけが固定である** | 表 A-2・B-2 は識別列 1、表 C・D-1・D-2 は識別列 2。既に不揃いである |
| 4 | **3 項関係は 2 枚に割り、合成は検査で結ぶ** | プロセス × エージェント × 方式は 1 枚にできない |

---

## 2. 差し替える表 D-1 —— `主担当` と `回る手順` を列にする

**識別列を 2 列足す。方式の 3 列と備考はそのままである。**

| # | プロセス | 主担当 | 回る手順 | 簡易 | 標準 | 厳格 | 備考 |
|:-:|---|---|---|:-:|:-:|:-:|---|
| 1 | トレーサビリティ管理 | test-designer | **無し** | 必須 | 必須 | 必須 | 仕様書の親子関係そのもの |
| 2 | 監査記録管理（decision） | project-manager | `2e` のみ | 必須 | 必須 | 必須 | 簡易は記録形式を簡略化してよい |
| 3 | ライセンス管理 | license-checker | `4f` | 必須 | 必須 | 必須 | 依存を足すときに動く |
| 4 | SCA（依存ライブラリの既知脆弱性調査） | security-reviewer | `4e` | 必須 | 必須 | 必須 | `npm audit` 等 |
| 5 | ユーザードキュメント・マニュアル作成 | user-manual-writer | `6f2` | 必須 | 必須 | 必須 | |
| 6 | 問題管理（defect / CR） | tester | **無し** | 条件付き | 必須 | 必須 | 簡易は同じ defect が再発したときのみ票を起こす |
| 7 | SAST（自コードの静的脆弱性解析） | security-reviewer | **無し** | 条件付き | 必須 | 必須 | CodeQL 等。簡易は外部入力を扱う場合のみ |
| 8 | 変更管理 | change-manager | `Fg` | 免除 | 必須 | 必須 | 簡易は仕様書を直接直す |
| 9 | リスク管理 | risk-manager | `3i` | 免除 | 必須 | 必須 | |
| 10 | コスト管理 | progress-monitor | `Fb` | 免除 | 必須 | 必須 | 記録先は表 D-2 の 22 |
| 11 | プロセス改善・再発防止（CAR / OPF） | process-improver | `Fe` `Ff` | 免除 | 必須 | 必須 | |
| 12 | ドキュメント版管理 | **各 file_type のオーナー** | **無し** | 免除 | 必須 | 必須 | 単一の担当を置かない唯一の行 |
| 13 | リリース管理 | project-manager | `6b`〜`6e` | 免除 | 条件付き | 必須 | 複数バージョンを並行保守するとき（標準） |
| 14 | ステークホルダー管理 | project-manager | **無し** | 免除 | 条件付き | 必須 | ステークホルダーが複数いるとき（標準） |
| 15 | API バージョニング・後方互換性 | architect | `3e` のみ | 免除 | 条件付き | 条件付き | 第三者に公開する API を持つ場合 |
| 16 | トレーニング・知識移転 | runbook-writer | `6f3` | 条件付き | 条件付き | 条件付き | 運用・保守フラグが有効で、運用チームへ引き継ぐとき |
| 17 | 並列実装（Git worktree） | project-manager | `3h` `4a` | 免除 | 免除 | 必須 | 1 つのリポジトリに作業ディレクトリを複数作り、各エージェントが専用ブランチで同時に書く |

**`回る手順` は現在の記号である。** `03-work-order.md` §6 の繰り上げでこの列は動く。検査 7 が追随を確かめる。

### 2.1 備考から消したもの

| 行 | 消した記述 | 消した理由 |
|:-:|---|---|
| 5 | `表 C-12 の根拠` | `主担当` 列が同じことを言う |
| 11 | `表 C-15・C-16 の根拠` | 同上。`decree-writer` は補助であり、補助は `05` が持つ |

---

## 3. 実測 —— 5 つのプロセスに手順が 1 つも無い

**`回る手順` 列を埋めて分かった。** `framework-src/ja/commands/full-auto-dev.md` を検索した結果である。

| 語 | 出現 |
|---|---:|
| `SAST` | **0** |
| `traceability` / `トレーサビリティ` | **0** |
| `stakeholder` / `ステークホルダー` | **0** |
| `document_version` / `版管理` | **0** |
| `defect`（defect 票を起こす手順として） | **0**（3 件あるがいずれも安全分析・曲線更新・根本原因分析） |

**これらは表 D-1 で必須と書かれているのに、パイプラインが 1 度も呼ばない。**

| # | プロセス | 表 D-1 の値 | 手順 |
|:-:|---|---|---|
| 1 | トレーサビリティ管理 | **全方式で必須** | 無し |
| 6 | 問題管理（defect / CR） | 標準・厳格で必須 | 無し |
| 7 | SAST | 標準・厳格で必須 | 無し（`4e` は SCA のみ） |
| 12 | ドキュメント版管理 | 標準・厳格で必須 | 無し |
| 14 | ステークホルダー管理 | 厳格で必須 | 無し |

**部分的にしか無いものが 3 件ある。**

| # | プロセス | 何が無いか |
|:-:|---|---|
| 2 | 監査記録管理 | `2e` が外部依存の選定結果を記録するだけ。他フェーズの decision を起こす手順が無い |
| 13 | リリース管理 | `6b`〜`6e` はデプロイの手順。`release-checklist` を作り判定する手順が無い |
| 15 | API バージョニング | `3e` は OpenAPI を生成するだけ。バージョニング戦略・非推奨通知ポリシーを定める手順が無い |

**この 8 件は `03-work-order.md` の作業項目になる。** 手順を新設するか、プロセス側を「手順を持たない継続的な規律」と定義し直すかは別途決める。

---

## 4. 差し替える表 C —— 備考から関係を抜く

**備考には条件と例外だけを残す。** 関係は他の表の列が持つ。

| # | エージェント | 簡易 | 標準 | 厳格 | 備考 |
|:-:|---|:-:|:-:|:-:|---|
| 1 | project-manager | ● | ● | ● | |
| 2 | srs-writer | ● | ● | ● | |
| 3 | architect | ● | ● | ● | |
| 4 | implementer | ● | ● | ● | |
| 5 | test-designer | ● | ● | ● | |
| 6 | tester | ● | ● | ● | |
| 7 | review-agent | ● | ● | ● | 回数と基準は表 E-1・E-2 |
| 8 | technical-authority | ● | ● | ● | ゲート判定 |
| 9 | license-checker | ● | ● | ● | |
| 10 | kotodama-kun | ● | ● | ● | |
| 11 | security-reviewer | ● | ● | ● | |
| 12 | user-manual-writer | ● | ● | ● | |
| 13 | risk-manager | — | ● | ● | |
| 14 | change-manager | — | 条件付き | 条件付き | 変更要求が出たとき |
| 15 | process-improver | — | ● | ● | |
| 16 | decree-writer | — | ● | ● | process-improver に従属 |
| 17 | progress-monitor | — | ● | ● | |
| 18 | runbook-writer | 条件付き | 条件付き | 条件付き | 運用・保守フラグ。フラグは方式と独立なので簡易でも起動しうる |
| 19 | incident-reporter | 条件付き | 条件付き | 条件付き | 同上 |
| 20 | field-test-engineer | 条件付き | 条件付き | 条件付き | 実機テストフラグ。同上 |
| 21 | feedback-classifier | 条件付き | 条件付き | 条件付き | 同上 |
| 22 | field-issue-analyst | 条件付き | 条件付き | 条件付き | 同上 |
| 23 | framework-translation-verifier | — | — | — | パイプラインでは起動しない。フレームワーク文書の保守用 |
| | 無条件で起動する体数 | 12 | 17 | 17 | |
| | 条件付きを含む上限 | 17 | 22 | 22 | |

### 4.1 備考から消したものと、その正本

| 消した記述 | 何行 | 正本 |
|---|:-:|---|
| `表 D-1 の N`（担うプロセス） | 6 | **表 D-1 の `主担当` 列** |
| `pipeline-state と decision を持つ` 等（持つ file_type） | 3 | **`agent-list.md` §2 file_type オーナーシップマトリクス** |
| `Ch9.1 / 10.1 / 11.1 のオーナー` 等（仕様書の章） | 2 | **`02-spec-writing-rules.md` 第 3 部の表** |
| `表 D-2 の 20`（持つ成果物） | 1 | **`agent-list.md` §2** |

**表 D-2 に `オーナー` 列を足してはならない。** 成果物のオーナーは `agent-list.md` §2 が正本であり、列を足すと三重になる。

---

## 5. 検査

### 5.1 検査 1 の文言を直す

現在は「全表の列が `簡易` / `標準` / `厳格` / `備考` である」。**識別列を否定してしまう。**

> **1** | **方式の列が** `簡易` / `標準` / `厳格` / `備考` の 4 つである。**識別列（`#`・名前・`主担当`・`回る手順` 等）は表ごとに異なってよい**

### 5.2 足す検査

| # | 判定 |
|:-:|---|
| 16 | 表 D-1 の全行に `主担当` が 1 つある。**`各 file_type のオーナー`（12 行目）を唯一の例外として許す** |
| 17 | 表 D-1 の `主担当` が表 C のエージェント名に実在する |
| 18 | **主担当が表 C でその方式に起動しないなら、そのプロセスは表 D-1 の同じ方式で `免除` である。** 逆向きには成り立たない（片側検査） |
| 19 | 表 D-1 の `回る手順` が `commands/full-auto-dev.md` に実在する。`無し` は実在の対象外とし、**件数を出力する**（現在 5 件） |
| 20 | 表 C の備考に `表 D-1 の` `表 D-2 の` という文字列が 0 件である（関係が備考へ戻っていないこと） |
| 21 | 既知の欠陥（存在しないエージェント名を `主担当` に 1 件仕込む）で FAIL する |

**検査 19 は `無し` を FAIL にしない。** 手順が無いこと自体は現状の記録であり、直すのは別の作業である。**黙って隠さず、数を出す。**

---

## 6. 表 B-2 は触らない —— 先の推奨を取り下げる

先に「表 B-2 に `プロセス` 列を足す」と書いたが、**取り下げる。**

**表 B-2 は全手順の表ではない。** 冒頭に「方式で差が出る手順と、条件で落ちうる手順を挙げる。ここに無い手順は全方式で実施する」とあり、22 行しかない。全 137 手順のうちの一部である。

**ここに `プロセス` 列を足しても、プロセス → 手順の対応は埋まらない。** 埋まるのは表 B-2 に載っている手順だけである。**方向を逆にして表 D-1 に `回る手順` 列を置くのが正しい**（§2）。こちらは 17 プロセスすべてを覆う。

**ただし表 B-2 の備考にも関係が 16 本ある**（表 D-1 へ 4 本、表 D-2 へ 12 本）。これらは成果物へのポインタが主であり、成果物のオーナーは `agent-list.md` §2 が持つ。**表 B-2 の扱いは別途決める。本書の対象外とする。**

---

## 7. 統合したら `05-process-agent-matrix.md` はどうなるか

| `05` の中身 | 行き先 |
|---|---|
| §2 の 17 件の主担当 | **表 D-1 の `主担当` 列へ。`05` から消す** |
| §3 の補助エージェント（7 プロセス分） | **`05` に残す。** 方式の列を持たない 2 項関係で、00 に置けない |
| §2 の「何をするか」 | プロセス規則 §3.2 の各節の中身。表ではない |
| §4 削る・割る・新設 | `03-work-order.md` へ移す。作業指示である |
| §5 表 C との突き合わせ | **検査 18 に置き換える。表に書かず機械に持たせる** |
| §6 気づいたこと | `History/2026-08-10/03-decision-record.md` へ |
| §7 検査 16〜20 | 本書 §5.2 が持つ（番号を振り直した） |

**結果、`05` は「補助エージェント表」＋「§3.2 各節の草案」に縮む。**
