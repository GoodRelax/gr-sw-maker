# 2 回目トライアル 観測ログ

**目的:** 2 回目トライアルの走行を観測し、フレームワークの是正が効いたかを記録する。**成果物の出来は対象外である。**

**位置づけ:** `2026-07-31/01-trial-report_v3.md` の着手順序 14（2 回目のトライアル）に対する観測記録。走行は別セッションが `gr-sw-maker-trial` で行い、本セッションは走らせない。

**規律:**

- **走行中に `framework-src/` を書き換えない（MUST NOT）。** 書き換えると、その走行が「どの版で走ったか」を失う。v3 §4.7 が G-1 を却下した理由である
- 走行中は追記のみ。フェーズ境界ごとに記録する
- **推測を結論として書かない。** 未解明は未解明と書き、仮説には印を付ける

---

## 1. 走行の同一性

| 項目 | 値 |
|---|---|
| フレームワーク HEAD | `7187bf8`（ブランチ `review-fixes`） |
| 走行対象 | `gr-sw-maker-trial`（unit-converter-cli） |
| `user-order.md` md5 | `0c3fd559d525bdd5a40659e26062e83e`（1 回目の原本と一致） |
| 1 回目の成果物 | `gr-sw-maker-trial_20260801_1.7z`（参照のみ。書き換えない） |

---

## 2. 開始前の確認（2026-08-02）

### 2.1 引継ぎ §3 の項目

| 確認 | 結果 |
|---|---|
| `git status --short` | 清潔 |
| `git rev-list --left-right --count origin/review-fixes...HEAD` | `0 0` |
| `check-parity` | PASS（41 ファイル対、ja vs en） |
| `check-roster` | PASS（44 エージェント定義） |
| `check-links` | PASS（118 リンク、269 節参照） |
| `check-tagnames` | PASS（74 タグ名参照） |
| `check-terms` | PASS（非採用語 30 件 × 74 ファイル） |
| `check-setup` | PASS（2 言語: en, ja） |
| `diff -rq framework-src` | 差分なし |

### 2.2 引継ぎ §2 の前提条件

| 確認 | 結果 |
|---|---|
| `user-order.md` md5 | 一致 |
| `session-state.json` | 不在（受け口未起動として正） |
| トライアル `.claude/settings.local.json` | 7 キー配置済み |
| ポート 4318 | 空き |
| **監視側 `.claude/settings.local.json`** | **`env` ブロックなし。** §2.1 の条件を満たす |

### 2.3 追加で確認した 2 点

**引継ぎ §3 に無い確認を 2 件行った。理由は §4-1 に記す。**

| 確認 | 結果 |
|---|---|
| 生成物の鮮度（`process-rules` 11 / `.claude/agents` 22 / `.claude/commands` 6 / `CLAUDE.md`） | **全件が `framework-src/ja` とバイト一致** |
| ~~トライアルの git 作業ツリー~~ | **訂正: この確認は誤りであった（下記）** |

> **訂正（Phase 0 中に判明）— 「トライアルの git 作業ツリー: 清潔」は誤りである。**
>
> **トライアルは git リポジトリではない。`.git` が存在しない。**
>
> 誤りの原因は確認コマンドの書き方にある。`git -C … status --short 2>/dev/null | head -20 || echo "(not a git repo)"` と書いたため、`||` が `git` ではなく `head` に掛かり、**git の失敗（exit 128）が握り潰されて空出力が「清潔」に見えた。** stderr も `2>/dev/null` で捨てていた。
>
> **走行セッションが Phase 0b2 で自ら検出し、報告してきたことで判明した。** 監視側が見落としたものを走行側が見つけた形である。

生成物に `project-manager.md`（E-2 改名の適用先）と `session-handoff.md`（P-20）が存在する。**是正後の版で走る状態にある。**

---

## 3. 監視の構成

### 3.1 計測経路の監視

**引継ぎ §4.3 は「フェーズ境界ごと」を定めるが、§4.1 の判定基準は 30 秒である。** 受け口が落ちてもフェーズ境界に着くまで検知できず、**1 回目の真因 D と同じ構造が残る。**

これを埋めるため、`session-state.json` を **60 秒ごとに読むだけの監視プロセス**を 1 本、リポジトリ外（スクラッチパッド）で走らせている。ユーザーの承認済み（間隔はユーザーの指定）。

| 制約 | 遵守 |
|---|---|
| 2 つ目の受け口を起動しない | ポートを開かない。読むだけである |
| `session-state.json` を書き換えない | 読み取り専用で開く |
| 生の OTel ペイロードをリポジトリ配下に書かない | ペイロードに触れない。`session-state.json` に PII は含まれない |
| 出力先 | スクラッチパッド（リポジトリ外）の JSONL |

**判定条件:**

| 条件 | 意味 |
|---|---|
| `MISSING` | ファイルが無い。受け口が起動していない |
| `SINK_STALE` | `sink_heartbeat_at` が 90 秒以上古い。受け口が落ちた（受け口の書込周期は 30 秒であり、90 秒は 3 回分の欠落） |
| `SINK_ALIVE_NO_EVENTS` | 受け口は生きているが `last_event_at` が null。送信側が無効 |
| `MULTI_SESSION` | `sessions_seen` が 2 以上。混線している |
| `OK` | 上記のいずれでもない |

### 3.2 報告の受け方

**ユーザーが本セッションを呼んだときにのみ観測する**（ユーザーの指定）。走行セッションからの報告はユーザーが中継する。

---

## 4. 気づき（フレームワークおよび手順の問題。**直さない**）

### 4-1. 引継ぎ §3 の確認は生成物の鮮度を保証しない

`diff -rq framework-src` は**原本しか見ない。** 走行が実際に読むのは `setup.js` の生成物である。**原本が一致していても生成物が古い版のままという状態が原理的に作れる。**

**実在した。** 監視側（`gr-sw-maker` 直下）の生成 `CLAUDE.md` が古く、文書管理規則を `v0.0.0` と書き、R2.1 Naming 節への導線を持たない。**原本は `v0.1.0` で導線を持つ。**

トライアル側は §2.3 のとおり全件一致であり、**走行への影響はない。**

**副作用が 1 つある。** 本セッションの文脈に注入される `CLAUDE.md` は古い方である。**規則解釈を答えるときは `framework-src/ja/` を引き、注入分を根拠に使わない。**

### 4-2. 検証項目 #18 / #19 は、このアプリでは規則の検証にならない

**走行前に記録する。走行後に書くと後知恵になる。**

`framework-src/ja/process-rules/review-standards.md` の Naming 節に次がある。

| 行 | 内容 |
|:-:|---|
| 89 | レイヤー別の命名表。**Use Case の例が `ConvertUnit`** |
| 82 | 単位の但し書き。**対象外の例が `Quantity { amount, unit }`** |

**トライアルは unit-converter-cli であり、規則の例示がこのアプリの正解そのものである。**

したがって architect が `ConvertUnit` と書き、`Quantity` を対象外と判定しても、**それは規則を読んだことの証明にしかならない。規則が未知のドメインで機能するかは判定できない。**

**通った場合は「例示の写経で説明がつく」と記録する。「規則が効いた」と書いてはならない。** 落ちた場合のみ強い情報を持つ（例示があってなお外した）。

**追記（走行開始後、Phase 0 時点）— #18 の識別力はさらに弱い。**

1 回目の成果物（`gr-sw-maker-trial_20260801_1.7z`。一覧のみ参照、展開せず）に次がある。

```text
gr-sw-maker-trial\src\usecase\convert-quantity.js
gr-sw-maker-trial\src\usecase\run-cli.js
```

**品詞規約は `b04c400` / `7187bf8` で入った。1 回目の後である。** つまり**規約が無い状態で既に Use Case が動詞句になっていた。**

**規則が無くても出ていたのだから、今回出ても規則の効果とは言えない。#18 は識別力を持たない。**

### 4-3. Micro の免除マトリクスが `cost-log.json` を免除し、予算の転記先が消える

| 出所 | 記述 |
|---|---|
| `full-auto-dev-process-rules.md` §3.1.1 免除マトリクス | コストログ（`cost-log.json`）: Micro = **免除** |
| `CLAUDE.md` 品質目標 | コスト予算は「**`cost-log.json` の `budget_usd` に転記する**」 |
| `full-auto-dev.md` Fb | 各フェーズ完了時に「`session-state.json` を読んで**コストを `cost-log.json` に追記する**」 |

**予算を設定しても転記先が免除されている。** アラートの分母を置く場所が無い。

**真因 D の対策として計測経路を通したが、最小規模では記録先が免除で抜ける。** P-01 の対策（D-1 / D-2）と §3.1.1 の免除マトリクスが突き合わされていない。**真因 C（改訂に突き合わせの義務がない）の新しい実例である。**

**本走行では直さない。** 総コストは §3.1 の監視ログから再構成できるため、検証項目 #1 は失われない。**規則の欠陥を抱えたまま走ることが観測である。**

### 4-4. Micro の所要時間基準が実態と 2 桁ずれている

§3.1.1 の規模区分は Micro を「**単一セッション内で完了（1 時間未満）**。単一モジュール、外部依存なし」と定める。例に「簡易 CLI ツール」が挙がっている。

**同じアプリの 1 回目の実績は 5 日である。**

規模区分の判定自体は妥当に見える（単一モジュール・外部依存なし・CLI）。**判定が正しいのに基準の数値が合わない。** P-05（規模区分が 1 段上に判定された）とは別の面であり、基準側の校正問題である。

### 4-5. 1 回目のブランチ戦略は記録されたが、一度も実行されていない

**7z を読み取りのみで実測した（展開は `.git/config` と `.git/HEAD` の 2 ファイル、スクラッチパッドへ）。**

| 観測 | 値 |
|---|---|
| `.git/config` | **`[remote]` セクションが無い** — GitHub 未登録 |
| `.git/HEAD` | `ref: refs/heads/master` — **`main` ですらない。git の既定のまま** |
| `.git/refs/heads` | **空**（ディレクトリのみ） |
| `.git/logs` | **不在**（reflog なし） |
| `.git/index`・`.git/objects` | **存在** |

**`git init` と `git add` は行われたが、コミットが 1 つも無い。**

一方、1 回目の `CLAUDE.md` はブランチ戦略を 9 行かけて読み替えて記録している。

```text
- メインブランチ: main（直接コミット禁止。PR マージのみ）
- 機能ブランチ: feature/{説明}（main から分岐）
- defect 修正ブランチ: fix/{説明}（main から分岐）
- 開発ブランチ（develop）: 設けない — 統合対象となる並行機能ブランチが存在しないため
- リリースブランチ: 設けない — タグによるリリース管理で足りるため
- PR マージ: feature/fix → main は review-agent PASS 後にのみ許可
```

**実態は `master`・コミット 0・ブランチ 0・PR 0 である。記録と実態が一致しない。**

利用者のグローバル規則（`~/.claude/CLAUDE.md`）は `git commit` / `git push` / `git tag` の実行を**全プロジェクトで禁じ**、`git add` のみ許している。走行セッションもこの規則の下にある。**`index` と `objects` があってコミットが無いという状態は、この規則と整合する。因果は断定しない。**

**この状態で 1 回目の GATE-DELIVERY は PASS している。** 記録された設定が実行されたかを検証する機構が無い。SAST も同型で、remote が無ければ GitHub Actions は起動しないが、`CodeQL（GitHub Actions で自動実行）` と記録され、後に waiver に至った。

**v3 の問題一覧に無い新規である。** 真因 C（改訂に突き合わせの義務がない）の変種に見えるが、**帰属は走行完了後に判定する。仮説として印を付ける。**

> **観測の汚染に注意。** 「ブランチ戦略は実行不能である」を走行セッションに伝えると、**2 回目が自力で気づくかどうかが観測できなくなる。** 走行セッションに渡してよいのは `git init` 済み・remote 無しという環境の事実までである。

### 4-6. 受け口の再起動が累計コストを黙って 0 に戻す

**2026-08-02、電源断を挟んで発生した。実測である。**

| 項目 | 値 |
|---|---|
| `sink_started_at`（当初） | `02:41:16Z` |
| `sink_started_at`（再起動後） | **`10:29:55Z`** |
| 再起動前の既知の最大値 | **$28.822632**（`10:01:03Z`、172 リクエスト） |
| 再起動後の値 | $47.485253（`16:36:13Z`、242 リクエスト） |

**機構:** `tools/otel-sink.mjs` はセッションごとの集計を**メモリ上のみ**で保持する。`recordApiRequest` は未知のセッション ID に対し `blankSession()`（全フィールド 0）を割り当て、`writeState()` は `{...readExisting(), ...session}` の順で展開する。**したがって再起動後の 0 始まりの集計が、ファイル上の累計を上書きする。**

**欠測が 2 つある。**

| # | 区間 | 状態 |
|:-:|---|---|
| 1 | `10:01:03Z` 〜 旧受け口の停止（最大 29 分） | **記録が上書きされて消えた** |
| 2 | 旧受け口の停止 〜 `10:29:55Z` の再起動 | **テレメトリごと捨てられた。永久に不明** |

**訂正 — 「監視ログにしか残っていない」と当初書いたが誤りである。** 走行側の `DEC-007` を読んで判明した。走行セッションは Phase 1 の消費を **`pre-crash 23.1852 + post-restart 24.3279`** と分けて記録しており、**再起動をまたいだ再構成に成功している。**

```text
cost-log の setup        $5.6374
cost-log の planning pre-crash  $23.1852
                        ─────────
                        $28.8226   監視ログの実測 $28.822632 と一致
```

**フレームワーク側も同じ値を保持していた。** 保持できたのは `cost-log.json` にフェーズ別の小計が書かれていたためであり、**その `cost-log.json` は Micro で免除されていたのに走行セッションが作ったものである**（§4-3）。**免除を無視した判断が、結果として計測を救った。**

**それでも受け口の欠陥は残る。** `cost-log.json` に書かれるのはフェーズ境界での小計であり、**フェーズの途中で受け口が落ちれば、その時点までの未記録分は失われる。** 今回は復元できたが、機構として保証されてはいない。

**これは真因 D と同じ型の新しい失敗である。** D-2 の鮮度マーカー（`sink_heartbeat_at`）は「受け口が**止まった**こと」を検知するが、「受け口が**再起動して累計を 0 に戻したこと**」は検知しない。**再起動後は `sink_heartbeat_at` が新鮮なので、判定は `OK` になる。** 手がかりは `sink_started_at` の変化だけであり、**それを比較する規約も機構も存在しない。**

**あわせて監視側の限界も記録する。** 本セッションの監視プロセスは同じ電源断で停止し、**6 時間 35 分（`10:01:03Z` 〜 `16:37:28Z`）の欠測を生んだ。** 監視プロセスは監視セッションの子プロセスであり、**セッションが落ちれば一緒に落ちる。** 再開時に `sink_started_at` の変化を検知して記録する処理を追加した（監視側の改修であり、`framework-src` には触れていない）。

---

## 5. フェーズごとの記録

### 5.1 Phase 0（`0-pre` 〜 条件付きプロセス評価）— 2026-08-02

**計測:**

| 項目 | 値 |
|---|---|
| 受け口の起動 | `2026-08-02T02:41:16Z` |
| `session_id` | `7d60a64a-…`。**`sessions_seen: 1`** — 混線なし |
| `last_event_at` | 受け口の初回書込から**非 null。** 送信側は最初から有効 |
| `total_cost_usd` | **$1.405967**（14 リクエスト。Phase 0 の質問到達時点） |
| `compaction_count` | 0 |
| `by_agent` | **空** |

**内訳が `03-measurement-path-poc.md` §3.4 の単価表と 6 桁一致した。**

```text
cache_read      1,241,734 × $0.50/M  = $0.620867
cache_creation     53,349 × $10.00/M = $0.533490
output             10,059 × $25.00/M = $0.251475
input                  27 × $5.00/M  = $0.000135
                                       ─────────
                                       $1.405967   実測 1.405967
```

**この時点では `cache_creation` は支配的でない（38%）。** 1 回目に観測した $8.63 の跳ね（99.7% が `cache_creation`）とは別の局面である。

`by_agent` が空であり、**サブエージェント別のコスト按分は未検証のまま**である（`03-measurement-path-poc.md` §7 未確定事項 #3）。Phase 1 以降で観測する。

**§5 の観測:**

| # | 項目 | 観測 |
|:-:|---|---|
| 1 | 総コスト | **測れている。1 回目の「計測不能」が解消した。** 最優先項目を満たした |
| 11 | `0-pre` | **ダイアログを出さずに起動し、確認まで到達した。** `env` は配置済みを検出し、再作成も Claude Code の再起動要求も発生しなかった。走行セッションが出した質問 4 件はいずれも `0-pre` ではなく `0b2` / `0o` 相当である |

**介入（引継ぎ §6.2 に基づく仕分け）:**

| 問い | 種類 | 回答と根拠 |
|---|---|---|
| 免除マトリクスをどこまで適用するか | 規則の解釈 | **フル適用でよいと答えた。** 根拠: §3.1.1 のルール「レビューは統合してよいが、スキップは禁止。**免除できるのは記録の形式と粒度であって、判定そのものではない**」。かつ `review-standards.md` のチェックリストは R-ID ごとに `Verdict` / `Remark` 列を持つ。**統合しても R3.4 / R3.5 / R3.6 の行は残り、検証項目 #15 / #16 / #17 は観測できる** |
| 実装言語（TypeScript か素の JS か） | 実験設計 | **素の JS を推した。走行セッションの推奨（TypeScript）と食い違う。** 根拠: 1 回目は素の JS（ESM）であり、TypeScript にすると型宣言でコード行数が増え、**検証項目 #2（仕様書 : コード = 2.66 : 1）の低下が R2.18 の効果か型宣言の嵩か区別できなくなる** |
| コスト予算 | **ユーザー判断** | **代理承認せず、材料を出して回した。** Phase 0 実績 $1.41 / 14 req からの外挿で全体 $15〜40（**仮説**）。25 USD なら中盤でアラートが発火する見込みがあり、**P-21 の配線が働くかを観測できる**と述べた |
| 条件付きプロセス 13 件すべて無効 | 規則の解釈 | **承認でよいと答えた。** CLI・単一実行・外部依存なし・人命/金銭/個人情報に無関係。**副次的に、運用・保守が無効 → Phase 7 スキップとなり、1 回目に FF-07 が起きたのと同じ条件が再現する。** 検証項目 #9（`project_status` に `completed` が書かれるか）を、壊れたときと同じ条件で観測できる |

**ユーザーの決定:** 免除フル適用 / 素の JavaScript（ESM）/ 25 USD・閾値 80% / 13 件すべて無効。

**気づき:** §4-3（`cost-log.json` 免除と予算転記先の矛盾）、§4-4（Micro の所要時間基準）、§4-2 への追記（#18 の識別力）。**いずれも記録のみ。`framework-src/` は未変更。**

### 5.2 Phase 1 完了・Phase 3（design）着手 — 2026-08-02

**進行:**

| 項目 | 値 |
|---|---|
| `phase` / `step` | **3（design）/ 3a**、`phase_progress: 0/9` |
| 通過したゲート | GATE-INTERVIEW = PASS、**GATE-PLANNING は TD-002 で一度 FAIL → 是正 → TD-003 で PASS** |
| Phase 2 | スキップ（条件付きプロセス 13 件すべて無効のため） |
| `compaction_count` | 0 |

**計測（下限のみ。§4-6 の欠測により総額は確定できない）:**

```text
再起動前の既知の最大値   $28.822632   10:01:03Z、172 リクエスト
再起動後の現在値         $48.153694   16:37:28Z、249 リクエスト
                        ──────────
実測できる下限            $76.98 以上、421 リクエスト以上
```

**予算は 25 USD。下限で 3 倍を超えている。**

サブエージェントの比率が上がっている。再起動後の内訳は `agent:custom` が **$37.50 / 217 リクエスト**、メインセッション（`sdk`）が $10.65 / 32 リクエスト。**サブエージェントが 78%** である（Phase 0 時点は 45%）。

**§5 の観測:**

| # | 項目 | 観測 |
|:-:|---|---|
| 5 | 従属記述の未追随（1 回目は design で 7 件） | **C-3 の追随確認が実際に発火している。** `pipeline-state.md` の `change_log` に、4 種（図 / ノート / 本文中の影響を述べた行 / ADR の Consequences / コード内の識別子）を**毎回名指しで列挙し、対象外なら理由を書いた**記録が残っている。**ただし「確認した」ことの観測であり、未追随が減ったかは design 完了後のレビュー指摘で判定する** |
| — | 自己検知 | `review-002` §9 #7 が `pipeline-state.md` の記載と実態の乖離を検出し、**対象文書外であることを理由に「指摘としない」と判定したうえで project-manager に更新を喚起**した。喚起に対する更新が実行されている |

**気づき:** §4-6（受け口の再起動が累計を黙ってリセットする）。**追随確認の記録が 1 回あたり数百字に達しており、コストの一因である可能性がある。仮説であり、切り分けていない。**

### 5.3 以降

**追記する。**
