このセッションの引継文書を作成してください:

0. **これはサブエージェントに委譲できない（MUST NOT）。** 会話履歴を持つのはメインセッションだけであり、以下の 2 項目はファイルからは復元できない
1. `project-management/pipeline-state.md` を読み、中断時点のフェーズと未完了タスクを確認する
2. **計画との差分**を洗い出す。計画書に書かれているが既に終わっているもの、計画になかったが実施したもの、計画したが不要と判明したもの
3. **自分で作り込んで自分で直した defect** を洗い出す。成果物には成功した最終状態しか残らないため、この経路はファイルに存在しない
4. 未解決の論点と、ユーザーとの合意事項のうちファイルに書いていないものを列挙する
5. `project-management/session-handoff/session-handoff-{連番}-{YYYYMMDD}-{HHMMSS}.md` に作成する。形式は文書管理規則 §9.38 に従う
6. `pipeline-state` の `latest_handoff` を作成したファイルのパスに更新する

**2 と 3 を省略してはならない（MUST NOT）。** 2 が無いと次のセッションが完了済みの作業を二重に行い、3 が無いと同じ踏み方をする。**素直に書くとこの 2 つが最初に抜ける。**
